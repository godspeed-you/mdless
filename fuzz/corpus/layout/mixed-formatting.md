P# Mixed Formatting

This paragraph mixes **bold**, *italic*, ***bold italic***, ~~strikethrough~~,
`inline code`, a [link](https://example.com "Example"), and an image
![icon](icon.png) inline.

**Bold with *nested italic* inside** and *italic with `code` inside*.

> A blockquote with **bold**, a [quoted link](https://example.org),
> and `code`.
>
> > Nested quote level two.

## Combinations in lists

1. **bold item**
2. *italic item* with ~~strike~~
3. `code item` and a [list link](#combinations-in-lists)

## Hard and soft breaks

line one with a hard break  
line two after hard break
soft-wrapped continuation line

## Horizontal rules

---

Text between rules.

***

## HTML passthrough

<div align="center">
  <b>raw html block</b>
</div>

Paragraph with <kbd>Ctrl</kbd>+<kbd>C</kbd> inline HTML.
